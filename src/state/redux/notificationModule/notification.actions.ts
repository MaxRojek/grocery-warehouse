import { createAction } from "@reduxjs/toolkit";


export const successAction: any = createAction("SUCCESS_ACTION");

export const alertAction: any = createAction("ALERT_ACTION");

export const errorAction: any = createAction("ERROR_ACTION");