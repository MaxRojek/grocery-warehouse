
import { useGlobalContext } from '../../state/contexts/contexts'

const defaultPage = () => {
  // const { login } = useGlobalContext()
  // const { state, dispatch } = login
  return (<p>default page</p>)
};


export default defaultPage;